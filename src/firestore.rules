
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // ---------------------------------------------------------------------------------------------
    // 🔹 Helper Functions
    // ---------------------------------------------------------------------------------------------

    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }
    
    function isCreator(docData) {
        return isOwner(docData.creatorId) || isOwner(docData.authorId) || isOwner(docData.userId);
    }

    function isSuperAdmin() {
      // This is a placeholder UID. In a real app, manage this securely.
      return request.auth.uid == "9RwsoEEkWPR3Wpv6wKZmhos1xTG2";
    }

    // ---------------------------------------------------------------------------------------------
    // 🔹 Public Read-Only Content
    // ---------------------------------------------------------------------------------------------
    // These collections can be read by anyone. Writes are handled by more specific rules below.
    match /{collectionId}/{docId} 
      where collectionId in [
        'deities', 'stories', 'epicHeroes', 'temples', 'rituals', 
        'festivals', 'panchang', 'products', 'shops', 
        'contests', 'challenges', 'channels', 'groups', 'manifestations', 'plans'
      ] {
      allow get, list: if true;
    }
    
    match /media/{mediaId} {
      allow get, list: if true;
    }

    // ---------------------------------------------------------------------------------------------
    // 🔹 Admin-Only Write Collections
    // ---------------------------------------------------------------------------------------------
    match /{collectionId}/{docId}
       where collectionId in ['deities', 'stories', 'epicHeroes', 'temples', 'rituals', 'festivals', 'panchang', 'products', 'shops', 'challenges', 'plans'] {
        allow write: if isSuperAdmin();
    }
    
    match /channels/{channelId} {
        allow write: if isOwner(channelId) || isSuperAdmin();
    }

    // ---------------------------------------------------------------------------------------------
    // 🔹 User Profiles & Subcollections
    // ---------------------------------------------------------------------------------------------
    match /users/{userId} {
      allow get: if true;
      allow create, update, delete: if isOwner(userId) || isSuperAdmin();

      match /followers/{followerId} {
        allow get, list: if true;
        allow create: if isSignedIn() && request.auth.uid == followerId && request.auth.uid != userId;
        allow delete: if isSignedIn() && (request.auth.uid == followerId || isOwner(userId));
      }

      match /following/{followedId} {
        allow get, list: if true;
        allow create, delete: if isOwner(userId) && request.auth.uid != followedId;
      }
       
      match /subscriptions/{channelId} {
        allow read, write: if isOwner(userId);
      }

      match /cart/{productId} {
        allow read, write: if isOwner(userId);
      }
      
      match /bookmarks/{bookmarkId} {
        allow read, write: if isOwner(userId);
      }
      
      match /{subcollection}/{docId} 
        where subcollection in ['preferences', 'horoscopes', 'badges', 'challenges', 'contestProgress', 'virtualOfferings', 'playlists'] {
        allow read, write: if isOwner(userId);
      }
    }
    
    // ---------------------------------------------------------------------------------------------
    // 🔹 User-Generated Content Collections
    // ---------------------------------------------------------------------------------------------
    // Rules for content any signed-in user can create.
    match /{collectionId}/{docId}
      where collectionId in ['media', 'posts', 'manifestations', 'playlists', 'groups', 'temple_renovation_requests', 'temple_maintenance_funds'] {
        allow get, list: if true;
        allow create: if isSignedIn();
        allow update, delete: if isCreator(resource.data) || isSuperAdmin();
    }

    // ---------------------------------------------------------------------------------------------
    // 🔹 Comments and Likes Subcollections
    // ---------------------------------------------------------------------------------------------
     match /{collectionId}/{docId} 
       where collectionId in ['media', 'posts', 'manifestations', 'temples'] {
      
      match /comments/{commentId} {
        allow get, list: if true;
        allow create: if isSignedIn();
        allow update, delete: if isCreator(resource.data) || isSuperAdmin();
      }

      match /likes/{userId} {
        allow get, list: if true;
        allow create, delete: if isOwner(userId);
      }
    }
    
    // ---------------------------------------------------------------------------------------------
    // 🔹 Special Collection Rules
    // ---------------------------------------------------------------------------------------------
    
    match /channels/{channelId}/subscribers/{subscriberId} {
         allow create, delete: if isOwner(subscriberId);
         allow get, list: if true;
    }
    
    match /groups/{groupId}/members/{userId} {
        allow read, write: if isOwner(userId);
    }

    match /contests/{contestId} {
        allow update: if isSignedIn() && request.resource.data.diff(resource.data).affectedKeys().hasOnly(['totalChants', 'updatedAt']);

        match /chants/{chantId} {
            allow create: if isSignedIn();
            allow read: if true;
        }
    }

     match /temple_maintenance_funds/{fundId}/donations/{donationId} {
        allow create: if isSignedIn();
        allow read: if isOwner(request.auth.uid) || isSuperAdmin();
     }
    
     match /orders/{orderId} {
      allow get: if isOwner(resource.data.userId) || isSuperAdmin();
      allow list: if isSuperAdmin();
      allow create: if isSignedIn();
      allow update: if isSuperAdmin();
    }

     match /transactions/{transactionId} {
        allow get: if isOwner(resource.data.userId) || isSuperAdmin();
        allow list: if isSuperAdmin();
        allow create: if isSignedIn();
        allow update: if isSuperAdmin();
    }
  }
}
